# BATCH-01：运行安全与统一验收基础

用户于 2026-09-06 明确要求一次多布置工作、一次集中验收，以减少交接轮次，尽快实现独立站正常使用并上线。本批取代 RT-01-R1 完成后必须停止、不得进入 RT-02/03 的旧安排；不取消安全要求。已完成的 R1 工作直接复用。

源码根：C:\Users\34707\Documents\ChatGPT\跨境电商\CrossBorder-Independent-Store。
文档根：源码根的父目录。应用根：源码根下 03_template/medusa-crossborder-base。
Review 根：C:\Users\34707\Documents\ChatGPT\跨境电商-review。

先读适用 AGENTS.md、实际工作树和 04_RUNTIME_DEPENDENCIES.md 的 RT-01～06。读取对应源码判断旧问题是否仍存在，不机械依赖报告行号。保存双仓库 HEAD/status/diff 和已有未提交文件 hash，保留已有工作。

## 连续执行的五项工作

### 1. 完成 RT-01-R1

按本目录 RT-01-R1.md 技术要求及 reviewer/RT-01/REVIEW_DECISION.md，返修测试隔离、错误原因断言、SKIP 计数及真实日志。旧任务的“只做本项然后停止”由本批取代，其余安全限制仍适用于此子项。完成后标记 READY_FOR_REVIEW，继续本批，无需等 Reviewer 单独批准。

### 2. RT-02：停止进程前验证身份

检查应用 scripts/_common.ps1 的 Start/Stop 管理流程、PID 文件及全部调用点，包括 build-production.ps1。禁止仅凭 PID 杀进程树。

记录并核对启动时间、可执行文件、项目/命令身份等足以区分进程的证据。旧格式或身份缺失/不匹配时拒绝终止，报告 stale，不猜测归属；实际终止前重新核对身份。子进程也不得因过期父子关系误杀。采用最小实现，不建立新进程管理框架。

验收只使用自建无害进程：正确归属可停；已退出、PID 指向另一进程、启动时间错误、不同项目、身份读取失败、旧格式均不误杀；外部哨兵进程存活。可注入身份记录模拟复用，无需等待系统真实 PID 复用。不得拿用户现有应用试杀。

### 3. RT-03：复用服务必须确认项目身份

检查 start-local.ps1、build-production.ps1 及相关 HTTP 等待/复用函数。HTTP 200 只作为健康证据，不能作为归属证据；核对监听进程、管理记录和项目启动身份。身份不足则报告冲突，既不复用也不抢占。已有身份信息足够时不新增生产 HTTP 端点；确需实例标识不得暴露绝对路径或凭据。

验收使用独占测试端口和受控服务：200 stub、错误项目/不匹配身份均不被复用或停止；正确实例可复用；端口未监听、服务未就绪、身份未知的状态区别清楚。

### 4. RT-04 / SF-11：统一验证入口与源码根 CI

核对实际 packageManager 和锁文件，使用既有包管理器。修复后端 Jest 未声明依赖入口及 Windows 环境变量语法；将真正存在的支付、商品、组件测试接入统一入口，明确两 app 的类型检查与 lint。

允许为这些明确问题调整 package scripts、Jest、Turbo、CI、必要直接依赖与工具生成的 lockfile；不得手改锁文件、跳过测试或关闭规则换取绿色结果。源码 Git 根设置适用 .github/workflows，不是只在模板嵌套目录放 workflow。CI 和本地调用同一入口；无测试执行不能 PASS。PowerShell 安全测试可独立 CI job，但不得静默跳过后声称全部通过。

构建类型/lint bypass 必须被实际强制检查覆盖或移除，失败阻断构建/发布入口；按实际框架版本实现，不生搬旧命令。已有问题只作必要修复，不批量重排格式。若完整 lint 暴露超出本批范围的业务问题，保留真实失败及定位，继续其他独立子项，不伪造全绿。

验收：真实测试运行、两 app 类型检查、lint、相关构建；在独立副本或受控注入中证明故意失败测试与类型错误使同一入口非零退出，之后验证最终工作树。不要在正式代码留下失败 fixture。CI 未推送实际运行则标记“已配置，远端未运行”。构建前确保停止流程安全，不为构建停止身份不明的服务。

### 5. 一次盘点上线前置条件

只读取已有非敏感配置、文档和现状，形成 EXTERNAL_PREREQUISITES.md：支付网关/商户市场与沙箱能力、已有商品真实库存/履约方式、域名/部署环境、邮件/客服、配送退款政策、备份恢复条件。每项标为已有证据/缺资料/需用户业务决定，并标注最迟阻塞阶段。

不重新选品，不编造成本、供货能力或商户资格，不要求用户发送密钥。技术工作不依赖这些外部事项时继续本批，整批报告中合并提出真正需决定的问题。形成 RT-05 后续依赖处置清单即可，本批不做无关升级或全局 major override。

## 范围与推进规则

- 本批授权上述运行脚本、测试、验证配置、必要依赖声明及最少相应文档变更。冻结架构和 UI，不改支付/商品业务行为，不迁移数据库、不发布、不真实收款、不发送邮件、不提交/推送/建 PR。
- 每项保留独立 diff/证据，子项仅 READY_FOR_REVIEW，Executor 不作最终 PASS。完成子项后继续本批依赖满足的工作，不逐项等待用户。
- 某项失败先自行修复；无法解除时停止依赖它的危险操作，继续其他安全独立工作。不能因一个外部条件缺失就停掉所有工作，也不能因批量授权跳过安全边界。
- 真实递归删除仅限事先独立验证了绝对边界、归属、reparse 的测试独占目录。禁止删除现有数据库/volume、正式资料或停止未知进程。
- 不修改 Reviewer 结论或自扩任务范围。必要 API/版本契约核对官方文档，不把历史报告当实时事实。

## 整批一次交付

证据目录：Review根/execution/BATCH-01/；子目录 RT-01-R1、RT-02、RT-03、RT-04。已有 R1 证据可按路径和 hash 引用，无需重跑或复制。

根目录交付 BATCH_REPORT.md、FINDING_MATRIX.md、EXTERNAL_PREREQUISITES.md、完整 diff（含 untracked）、双仓库 status/HEAD。子项保留原始 stdout/stderr、命令、shell、时间、退出码、文件 hash、测试数及跳过原因。矩阵逐项写 CLOSED_CANDIDATE、BLOCKED 或 NOT_RUN 并链接证据；都不等于 Reviewer PASS。

整批完成或已无安全独立工作可做时，再汇报 BATCH-01 READY_FOR_REVIEW 或 PARTIAL/BLOCKED。最多八行：整体链路、子项状态、证据目录、需用户决定事项。进度可简短更新，但不得每修一个小点就结束等待用户。

Reviewer 集中检查同一最终工作树，逐子项 PASS/RETURN，独立复核关键反例与统一入口，一次给齐剩余返修或下一批任务。不为某项返修重验全部未变化的已通过内容。