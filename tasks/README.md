# tasks/

Reviewer-authored Gate prompts and execution packages live here.

A task file defines scope for one Gate, but it is not current authority by itself. The active Gate must be named by root `REVIEWER_HANDOFF.md`.

Closed or returned task files remain historical and must not be resumed automatically.
