# 当前执行任务：RT-01-R1

状态：READY_FOR_EXECUTION。Reviewer 结论：RT-01 RETURN。最终目标：独立站正常使用并上线。

工作区：`C:\Users\34707\Documents\ChatGPT\跨境电商`。
源码：工作区下 `CrossBorder-Independent-Store`。
Review 根：`C:\Users\34707\Documents\ChatGPT\跨境电商-review`。

先读取适用 AGENTS.md、Review 根下 `reviewer/RT-01/REVIEW_DECISION.md` 和实际工作树；无需用户重复解释或确认。当前已有两个 RT-01 未提交变更，不回退、不覆盖他人变更。

## 本轮唯一目标

修复 RT-01 安全测试与证据的三个问题。主体删除保护已通过 Reviewer 的 19 项独立探针，优先保持 `04_docs/scripts/92-prepare-review-staging.ps1` 字节不变。只修改 `04_docs/scripts/92-prepare-review-staging.security.tests.ps1`；确有必要调整主脚本时，必须直接对应具体失败证据，禁止顺带重构。

1. 真正的源码、文档中心、祖先、用户目录、盘根等危险路径只传给实际路径校验函数。可通过 AST 提取函数定义，参考 Reviewer 探针；不得 dot-source 整个脚本或启动完整入口来验证这些负例。即使安全 guard 失效，负例测试也不得访问实际删除/复制入口。
2. 文件系统测试仅使用测试独占目录。开始写入和 cleanup 前独立验证绝对路径、归属以及已有父路径 reparse；cleanup 不得依赖被测 guard 的正确性。合法创建/重跑可调用完整脚本，但只能传上述验证过的测试 staging，以及独立 package/manifest 路径。其他目录仅只读。
3. 负例断言明确的安全错误原因，非预期异常直接 FAIL。保留正常创建/重跑、无 marker、错任务/路径 marker、损坏 JSON、junction 及其目标未受影响的验证。路径链和目录内部 junction 可用函数层验证。不要只检查非零退出码。
4. PASS/FAIL/SKIP 分开计数；junction 无法创建时真实报告未测，不纳入 PASS。symbolic-link 未测单列即可。增加小型无副作用的断言自校验：注入一个普通运行异常时，拒绝断言必须失败；测试框架能正确识别该预期失败。无需新框架或外部依赖。
5. 执行安全测试，并原样捕获 stdout/stderr。不要手写/改写 RAW_TEST_LOG。另记实际命令、shell 版本、时间、退出码、源文件 SHA256、断言数量与跳过项。报告区分内容比较和实际 hash 验证。

## 证据与结束条件

全部新证据放 `Review根/execution/RT-01-R1/`：`EXECUTION_REPORT.md`、`RAW_TEST_LOG.txt`、`RUN_METADATA.json`、`ACTUAL_DIFF.patch`（包含 untracked 测试内容）、`GIT_STATUS.txt`。不要覆盖 RT-01 原交付和 Reviewer 文档。

确认文档仓库 CLEAN、两个 HEAD 未变化，源码改动范围符合任务；不提交、不推送、不建 PR，不进入 RT-02/RT-03，不启动真实支付、数据库迁移或生产部署，不修改冻结 UI。

完成后状态仅为 `READY_FOR_REVIEW`，由 Reviewer 独立决定 PASS/RETURN。用最多六行报告：任务状态、通过/失败/跳过数、证据目录、变更范围、剩余问题、是否需要用户介入。技术细节写文件。失败时自行定位并在本轮范围内修复重试，不反复询问实现细节；只有业务决策、缺少凭据或超出授权的操作才需要用户。
