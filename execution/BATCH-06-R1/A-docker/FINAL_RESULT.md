# A — Docker / Medusa production artifact

Observed final image: `cb-batch06-r1-medusa:20260908`.

- Real Docker multi-stage build completed with exit code `0`; the no-cache run used the mirror-resolved base and completed the locked 1481-package install. See `docker-build-clean-no-pull-final.log` and `docker-build-clean-no-pull-final.exitcode.txt`.
- Direct Docker Hub metadata access was unavailable. The same build completed through the accessible registry mirror, with base image digest `sha256:1c18d9ab3af4585870b92e4dbc5cac5a0dc77dd13df1a5905cea89fc720eb05b`; see `registry-mirror-pull.log`, `base-image-inspect-current.txt`, and the build log.
- A direct `--pull` attempt still failed at Docker Hub token authorization; the successful no-cache run used the already mirror-resolved base digest and completed without layer cache reuse. The direct-auth failure is retained in `docker-build-clean-no-cache-final.log`.
- Current Docker image ID/repo digest is `sha256:a64e3d304a30b122b7f15192facc99e0deb731ae70d2baacd51c543327c49987`; the current saved manifest contains 14 compressed layer blobs. The saved image tar, manifest, and runtime identity are `image-save-current.tar`, `image-manifest-current.json`, and `image-runtime-metadata-current.txt`.
- Runtime identity is non-root `medusa` (`uid=10001`). The artifact directory is `/app/apps/backend/.medusa/server`; its `medusa-config.js`, `package.json`, backend CLI, and `/app/entrypoint.sh` are present. The runtime workdir is the artifact directory and the entrypoint changes into it before dispatch.
- With an isolated PostgreSQL/Redis network and `sslmode=disable` for the local plain PostgreSQL container, the current image tag `migrate` exited `0`; its server and worker stayed running after startup, the server loopback health returned `200 OK`, and both exited `0` on graceful stop. See `../C-integration/medusa-migrate-current-tag-current.log`, `../C-integration/runtime-current-tag-current.txt`, and `../C-integration/runtime-container-shutdown-current.txt`.
- Production boundary checks reject `MEDUSA_LOCAL_RUNTIME=true` and reject missing `MEDUSA_PRODUCTION_INFRASTRUCTURE_ENABLED`; see `production-local-runtime-rejected-current.txt` and `production-infrastructure-required-current.txt`. A production-hosted Redis/S3 runtime was not claimed because no approved production infrastructure was available.
- The current artifact/entrypoint filesystem scan and all 14 current image layers found zero high-confidence secret-value or PEM-payload matches; no secret values were emitted. See `artifact-secret-scan-current-tag.txt`, `image-layer-scan-summary-current.txt`, `image-layer-secret-value-scan-current.txt`, and `image-save-current-file-list.txt`.
- The reconciliation runtime fix was compiled by the local backend build with exit code `0`, then packaged in a real incremental Docker build as `cb-batch06-r1-medusa:20260908-patched` from the verified 14-layer parent. The patched image has 16 layers, retains the non-root artifact workdir/entrypoint, and its production `migrate`, `server`, and `worker` checks used the isolated TLS Redis runtime; see `docker-build-durable-patch-image-current.log`, `../C-integration/production-tls-patched-migrate-current.log`, `../C-integration/production-tls-patched-server-current.log`, and `../C-integration/production-tls-patched-worker-current.log`.

No Live secret was read, copied, stored, or called.
