# C — Isolated integration evidence

- Network: `cb-b06r1-isolated-20260908`.
- Fresh PostgreSQL 16 and Redis 8 containers were created without a volume; readiness evidence is in `postgres-ready.log`, `redis-ready.log`, and `isolated-services-final-status.txt`.
- Current-tag Medusa migration completed with exit code `0` against the isolated database. The initial timeout was reproduced and diagnosed as TLS against the deliberately plain local PostgreSQL endpoint; rerun with `?sslmode=disable` completed all module migrations, link sync, and migration scripts.
- Current-tag server and worker each remained running after startup as non-root processes; the server loopback health returned `200 OK`, and both exited `0` after graceful stop. See `runtime-current-tag-current.txt` and `runtime-container-shutdown-current.txt`.
- Real Store API calls through the current image-tag server, with a local publishable key kept out of logs, returned `200` for health, regions, products, payment providers with region, custom route, cart creation, line-item creation, cart read-back, and shipping options with cart; see `store-api-current-tag-with-key-current.txt` and its exit code.
- The current image-tag Store API cart/line-item flow returned `200` for region lookup, cart creation, product lookup, line-item creation, and cart read-back; database read-back observed 4 non-deleted carts, 2 cart line items, 20 inventory items, 20 inventory levels, 20 positive-stock levels, and 0 reservation items; see `store-api-current-tag-db-readback.txt`.
- A separate production-configured run used an ephemeral self-signed TLS Redis container and the patched image. Redis cache, event-bus, locking, and workflow-engine connections established successfully; patched server `/health` returned `200 OK`, patched worker stayed running, and patched migrate completed with exit code `0`. See the `production-tls-patched-*` evidence files.

The compiled reconciliation module was then loaded with PayPal enabled using process-local test values. Its real isolated PostgreSQL persistence path passed Webhook dedupe/status transitions and refund operation status/read-back using a local deterministic retrieve seam; see `../B-paypal-durable/isolated-db-durable-fixed-current.log`. This is not signed provider evidence. PayPal provider Webhook/refund and provider-side read-back were not executed because no external Sandbox receiver/merchant chain was available.
