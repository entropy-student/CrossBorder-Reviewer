# PayPal durable local result

The reconciliation module was loaded from the compiled artifact against the isolated PostgreSQL database. After correcting the Medusa dynamic CRUD method casing and adding the required timestamp/soft-delete columns, the real DB path passed: first Webhook persistence was `verified`, the duplicate was replayed without a second claim, the lifecycle reached `dispatch_requested>applied`, and a registered refund operation reached `completed` after a local read-back seam. The patched production image repeated the module load and assertions with isolated TLS Redis and a successful production migration check. An eight-request concurrent fixed-event run produced one claim, seven replays, one database row, and zero rejections; a second independent process read the same event as a replay. Evidence: `isolated-db-durable-fixed-current.log`, `isolated-db-durable-patched-image-production-current.log`, `isolated-db-concurrency-result-current.md`, `paypal-enabled-migrate-patched-artifact-current.log`, and `paypal-enabled-patched-image-production-migrate-current.log`.

The refund read-back in this local check was an in-process deterministic seam, not a provider response; signed provider Webhook verification, public receiver delivery, and provider-backed refund/read-back remain unexecuted because the approved Backend host/DNS/TLS/Sandbox merchant prerequisites are unavailable. No simulated result is represented as real PayPal evidence.
